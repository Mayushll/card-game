import styled from "styled-components"

export const SWrapper = styled.div`
    position: relative;

    width: 100%;
    height: 70vh;
    padding: 25px;
    display: flex;
    justify-content: space-around;
    align-items: center;
    flex-direction: row;
    flex-wrap: wrap;
    overflow: hidden;
`
