import styled from "styled-components"

export const SQuitButton = styled.div`
    cursor: pointer;
    position: absolute;
    bottom: 15px;
    left: 15px;
    z-index: 900000;
    color: hsl(var(--white));
    font-size: 25px;
`
